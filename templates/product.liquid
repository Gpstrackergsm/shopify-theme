{%- comment -%}
  Product template displaying images, product information, and add-to-cart form.
{%- endcomment -%}
<section class="product-landing" itemscope itemtype="https://schema.org/Product">
  <div class="product-landing__hero" id="Product-{{ product.id }}">
    <div class="product-landing__copy">
      <p class="product-landing__eyebrow">The 21-Day Challenge Workbook</p>
      <h1 class="product-landing__title" itemprop="name">Discover the viral 18-10-8-4-1 formula that’s transforming lives worldwide.</h1>
      <p class="product-landing__lede" itemprop="description">
        This 25-page digital workbook helps you lose up to 7 kg in 3 weeks through a simple daily system: 18 hours fasting, 10 000 steps, 8 hours sleep, 4 liters hydration, and 1 gram of protein per kg. Track your progress, stay motivated, and reset your body naturally — no starvation, no confusion, just results.
      </p>
      <ul class="product-landing__highlights">
        <li>📘 Structured 25-page workbook with checklists and reflection prompts.</li>
        <li>🧠 Science-backed routine designed to reset your metabolism without guesswork.</li>
        <li>💪 Built-in habit trackers to keep you accountable every single day.</li>
      </ul>
      <div class="product-landing__purchase" itemprop="offers" itemscope itemtype="https://schema.org/Offer">
        <div class="product-landing__price">
          <span class="product-landing__price-label">Instant download</span>
          <div class="product-landing__price-value">
            <link itemprop="availability" href="https://schema.org/{% if product.available %}InStock{% else %}OutOfStock{% endif %}">
            <span itemprop="priceCurrency" content="{{ cart.currency.iso_code }}"></span>
            <span itemprop="price">{{ product.price | money }}</span>
          </div>
        </div>
        {% form 'product', product, class: 'product-landing__form' %}
          {% unless product.has_only_default_variant %}
            <div class="product-landing__options">
              {% for option in product.options_with_values %}
                <label for="Option-{{ option.position }}">{{ option.name }}</label>
                <select id="Option-{{ option.position }}" name="options[{{ option.name }}]">
                  {% for value in option.values %}
                    <option value="{{ value | escape }}"{% if option.selected_value == value %} selected{% endif %}>{{ value }}</option>
                  {% endfor %}
                </select>
              {% endfor %}
            </div>
          {% endunless %}
          <div class="product-landing__actions">
            <label for="Quantity-{{ product.id }}" class="visually-hidden">{{ 'products.product.quantity' | t }}</label>
            <input id="Quantity-{{ product.id }}" name="quantity" type="number" min="1" value="1">
            <button type="submit" class="button button--primary" {% unless product.available %}disabled{% endunless %}>
              {% if product.available %}
                {{ 'products.product.add_to_cart' | t }}
              {% else %}
                {{ 'products.product.sold_out' | t }}
              {% endif %}
            </button>
          </div>
        {% endform %}
        <p class="product-landing__guarantee">🔒 Secure checkout · 📥 Instant access · ♻️ Lifetime updates included</p>
      </div>
    </div>
    <div class="product-landing__media">
      {% if product.featured_media %}
        <figure class="product-landing__figure" data-media-type="{{ product.featured_media.media_type }}">
          {{ product.featured_media | media_tag: image_size: "1024x" }}
        </figure>
      {% elsif product.media.size > 0 %}
        {% assign first_media = product.media.first %}
        <figure class="product-landing__figure" data-media-type="{{ first_media.media_type }}">
          {{ first_media | media_tag: image_size: "1024x" }}
        </figure>
      {% else %}
        <div class="product-landing__placeholder" role="presentation"></div>
      {% endif %}
    </div>
  </div>

  <div class="product-landing__formula" aria-labelledby="formula-heading">
    <h2 id="formula-heading">Your daily 18-10-8-4-1 rhythm</h2>
    <div class="product-landing__formula-grid">
      <div class="product-landing__formula-card">
        <span class="product-landing__formula-number">18</span>
        <p>hours of intermittent fasting to unlock metabolic flexibility.</p>
      </div>
      <div class="product-landing__formula-card">
        <span class="product-landing__formula-number">10k</span>
        <p>steps to keep your energy high and your body in motion.</p>
      </div>
      <div class="product-landing__formula-card">
        <span class="product-landing__formula-number">8</span>
        <p>hours of sleep so your hormones and hunger cues stay balanced.</p>
      </div>
      <div class="product-landing__formula-card">
        <span class="product-landing__formula-number">4</span>
        <p>liters of hydration to flush toxins and sustain focus all day.</p>
      </div>
      <div class="product-landing__formula-card">
        <span class="product-landing__formula-number">1</span>
        <p>gram of protein per kg to rebuild lean muscle and curb cravings.</p>
      </div>
    </div>
  </div>

  <div class="product-landing__features" aria-labelledby="features-heading">
    <div class="product-landing__feature-intro">
      <h2 id="features-heading">What’s inside the workbook</h2>
      <p>Every page guides you through the challenge with clarity, structure, and encouragement.</p>
    </div>
    <div class="product-landing__feature-grid">
      <article class="product-landing__feature-card">
        <h3>Daily dashboards</h3>
        <p>Plan your meals, track fasting windows, log hydration, and celebrate every win in one view.</p>
      </article>
      <article class="product-landing__feature-card">
        <h3>Progress checkpoints</h3>
        <p>Weekly reflections and measurements keep you aligned with your goals and highlight momentum.</p>
      </article>
      <article class="product-landing__feature-card">
        <h3>Mindset coaching</h3>
        <p>Motivational prompts, affirmations, and habit cues prevent burnout and boost consistency.</p>
      </article>
      <article class="product-landing__feature-card">
        <h3>Ready-to-use resources</h3>
        <p>Printable trackers, shopping checklists, and meal ideas make the 18-10-8-4-1 method effortless.</p>
      </article>
    </div>
  </div>

  <div class="product-landing__how" aria-labelledby="how-heading">
    <h2 id="how-heading">How it works</h2>
    <ol class="product-landing__timeline">
      <li>
        <h3>Download instantly</h3>
        <p>Open the PDF on any device or print it out and prepare for day one of the challenge.</p>
      </li>
      <li>
        <h3>Follow the daily rhythm</h3>
        <p>Use the workbook prompts to lock in the 18-10-8-4-1 routine and stack habits effortlessly.</p>
      </li>
      <li>
        <h3>See results in weeks</h3>
        <p>Stay accountable, review your progress, and watch up to 7 kg disappear in just 21 days.</p>
      </li>
    </ol>
  </div>

  <div class="product-landing__social-proof">
    <h2>Real people, real transformations</h2>
    <div class="product-landing__quotes">
      <blockquote>
        “Week two and I’m already down 4 kg. The checklist format keeps me focused without feeling overwhelmed.”
        <cite>— Sofia M.</cite>
      </blockquote>
      <blockquote>
        “The 18-10-8-4-1 method finally made weight loss simple. Hydration and sleep tracking were game changers.”
        <cite>— Daniel K.</cite>
      </blockquote>
      <blockquote>
        “I’ve tried every plan. This one actually fits my life and the workbook makes it fun to stay consistent.”
        <cite>— Priya S.</cite>
      </blockquote>
    </div>
  </div>

  <div class="product-landing__faq" aria-labelledby="faq-heading">
    <h2 id="faq-heading">Frequently asked questions</h2>
    <div class="product-landing__faq-grid">
      <details>
        <summary>Do I need special equipment or apps?</summary>
        <p>No extras required. A pedometer or phone step counter is helpful, but everything you need is inside the workbook.</p>
      </details>
      <details>
        <summary>Is the workbook suitable for beginners?</summary>
        <p>Absolutely. The 18-10-8-4-1 routine is broken into micro-actions so you can build momentum without burnout.</p>
      </details>
      <details>
        <summary>How fast do I get access?</summary>
        <p>You receive a download link immediately after checkout so you can start the 21-day challenge today.</p>
      </details>
      <details>
        <summary>Can I reuse the workbook?</summary>
        <p>Yes! Print new copies or duplicate the digital pages to repeat the challenge anytime you need a reset.</p>
      </details>
    </div>
  </div>
</section>
